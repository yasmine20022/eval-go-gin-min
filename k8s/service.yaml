apiVersion: v1
kind: Service
metadata:
  name: eval-go-gin-min
  labels:
    app.kubernetes.io/name: eval-go-gin-min
    app.kubernetes.io/component: web
    app.kubernetes.io/part-of: eval-go-gin-min
spec:
  type: NodePort
  selector:
    app: eval-go-gin-min
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
